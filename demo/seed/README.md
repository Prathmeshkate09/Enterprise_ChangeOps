# Demo seed

Versioned, disclosed synthetic seed data lives in
`enterprise-sandbox/src/changeops_sandbox/seed`. Each tenant receives an
independent copy through an explicit reset operation; API results are derived
from that state rather than hardcoded demo responses.
