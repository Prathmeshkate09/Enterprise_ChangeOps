# Support sandbox

Functional synthetic support configuration service with inspect, snapshot,
dry-run patch, idempotent apply, restore, lookup verification, and reset
operations. The independently deployable service runs on port 8103 in Compose
and preserves the disclosed dependent forms in its tenant-scoped state.
