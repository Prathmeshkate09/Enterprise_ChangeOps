# Python contracts

Versioned, immutable Pydantic contracts for Enterprise ChangeOps domain events,
plans, approvals, policy decisions, tool intents, and audit records.

Unknown fields are rejected. All timestamps must include a timezone. Contract
models contain no authorization or workflow decision logic.
