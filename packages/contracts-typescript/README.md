# TypeScript contracts

Strict TypeScript representations of the versioned wire contracts plus the
same canonical JSON profile used by the Python plan hasher. The package has no
UI dependency and can be consumed by server or browser code.
